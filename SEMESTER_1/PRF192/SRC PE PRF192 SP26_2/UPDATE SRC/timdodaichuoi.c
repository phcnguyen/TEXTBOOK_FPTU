#include <stdio.h>
#include <string.h>

int main() {
    char str[] = "Độ dài chuỗi này là bao nhiêu?";

    int length = strlen(str); // Tính độ dài chuỗi

    printf("Độ dài chuỗi: %d\n", length);

    return 0;
}